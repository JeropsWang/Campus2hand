server:
  port: 8081

spring:
  application:
    name: user-service
  cloud:
    nacos:
      discovery:
        server-addr: 192.168.0.3:8848
        username: nacos
        password: nacos
      config:
        on-profiles: dev
        server-addr: 192.168.0.3:8848
        file-extension: yml
        username: nacos
        password: nacos

      shared-configs:
        - data-id: common-db.yaml
          group: DEFAULT_GROUP
          refresh: true
