spring:
  application:
    name: product-service  # 服务名，必须写
  #  profiles:             # 环境，规则就是{文件名}-{环境}.格式
  #    active: dev

  cloud:
    nacos:
      discovery:
        server-addr: 10.125.137.99:8848  # Nacos 地址
        enable: true
        file-extension: yaml         # 配置文件的格式